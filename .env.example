# Development Configuration
# Copy this file to .env and update values for your local environment

# Developer Name (used to track progress in docs/progress/)
DEVELOPER_NAME=bwyoon

# ============================================================
# Docker & Database Configuration
# ============================================================
# Database Credentials (for Docker setup)
DB_USER=slea_user
DB_PASSWORD=change_me_dev_password
DB_NAME=sleassem_dev
DB_HOST=db
DB_PORT=5432

# Database Connection Strings
# ⚠️ IMPORTANT: Use postgresql+asyncpg for async SQLAlchemy (FastAPI compatible)
# Format: postgresql+asyncpg://user:password@host:port/dbname

# Option 1: Local PostgreSQL (Current setup - RECOMMENDED)
# Use this if PostgreSQL is installed locally on WSL
DATABASE_URL=postgresql+asyncpg://himena:change_me_strong_pw@localhost:5432/sleassem_dev
TEST_DATABASE_URL=postgresql+asyncpg://himena:change_me_strong_pw@localhost:5432/sleassem_test
PROD_DATABASE_URL=postgresql+asyncpg://himena:change_me_strong_pw@localhost:5432/sleassem_prod

# Option 2: Docker PostgreSQL (For Docker Compose)
# Uncomment if using docker-compose setup
# DATABASE_URL=postgresql+asyncpg://slea_user:change_me_dev_password@db:5432/sleassem_dev
# TEST_DATABASE_URL=postgresql+asyncpg://slea_user:change_me_dev_password@db:5432/sleassem_test
# PROD_DATABASE_URL=postgresql+asyncpg://slea_user:change_me_dev_password@db:5432/sleassem_prod

# ⚠️ CRITICAL: Always use postgresql+asyncpg (not just postgresql)
# - postgresql+asyncpg: Async driver for FastAPI
# - postgresql: Sync driver (will block async event loop!)

# ============================================================
# Proxy Configuration (사내 환경에서만 필요)
# ============================================================
# 사외(집): 비워두기
# 사내(회사): 회사 proxy 정보 입력

# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,127.0.0.1,.company.com,db

# ============================================================
# PyPI Mirror Configuration (사내 환경에서만 필요)
# ============================================================
# 사외(집): 비워두기 (기본 PyPI 사용)
# 사내(회사): 회사 PyPI 미러 정보 입력

# PIP_INDEX_URL=http://pypi.company.internal:8080/simple
# PIP_EXTRA_INDEX_URL=http://mirror.company.internal:8080/pypi/simple

# LLM Configuration
# ==================
# Choose ONE of the following two configurations:

# Option 1: Google Generative AI (Gemini) - DEFAULT
# Set USE_LITE_LLM=False and provide GEMINI_API_KEY
GEMINI_API_KEY=your-api-key

# Option 2: LiteLLM (Proxy Server) - Supports multiple backends
# Set USE_LITE_LLM=True and provide LiteLLM configuration
USE_LITE_LLM=False

# LiteLLM Configuration (only used if USE_LITE_LLM=True)
# =====================================================
# Base URL of LiteLLM proxy server
# Example: http://localhost:4444/v1
LITELLM_BASE_URL=http://localhost:4444/v1

# API key for LiteLLM (can be any value, not validated by proxy)
# Example: sk-4444
LITELLM_API_KEY=sk-4444

# Model name to use with LiteLLM
# Examples: gemini-2.5-pro, gpt-4o, claude-3-sonnet, qwen-14b
LITELLM_MODEL=gemini-2.0-flash

# Environment
APP_ENV=dev

# Azure AD (if needed)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret
